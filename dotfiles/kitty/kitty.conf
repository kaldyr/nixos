include themes/frappe.conf

# System
enable_audio_bell no
linux_display_server wayland

# Fonts
font_family family="Maple Mono NF" postscript_name=MapleMono-NF-Regular features="+calt +cv02 +cv03 +cv05 +cv07 +cv09 +cv10 +cv31 +cv38 +cv40 +cv42 +cv43 +cv62 +ss03 +ss07 +ss08 +ss11 +zero"
bold_font auto
italic_font auto
bold_italic_font auto
font_size 10.6
disable_ligatures never

# Cursor
cursor_shape beam
cursor_beam_thickness 1.8

# Tab Bar
tab_bar_style powerline
tab_powerline_style round
tab_title_max_length 15

# URLs
detect_urls yes
url_style curly

# Integrations
editor nvim
scrollback_pager sh -c 'nvim 63<&0 </dev/null -c -i NONE "au VimEnter * term cat /dev/fd/63 && sleep 1000"'
shell fish
shell_integration enabled

# Keybindings
clear_all_shortcuts yes

# General
map alt+shift+\ load_config_file
map alt+equal   change_font_size all +1.0
map alt+minus   change_font_size all -1.0
map alt+0       change_font_size all 0

# Layout
map alt+[       goto_layout fat
map alt+]       goto_layout tall
map alt+\       toggle_layout stack

# Scrollback
map alt+up      scroll_line_up
map alt+down    scroll_line_down
map alt+u       scroll_page_up
map alt+d       scroll_page_down
map alt+e       show_scrollback

# Tabs
map alt+t       new_tab
map alt+.       next_tab
map alt+,       previous_tab
map alt+shift+t set_tab_title
map alt+shift+. move_tab_forward
map alt+shift+, move_tab_backward

# Windows
map alt+n       new_window
map alt+x       close_window
map alt+r       start_resizing_window
map alt+s       swap_with_window
map alt+f       focus_visible_window
map alt+h       neighboring_window left
map alt+j       neighboring_window down
map alt+k       neighboring_window up
map alt+l       neighboring_window right
map alt+shift+h move_window left
map alt+shift+j move_window down
map alt+shift+k move_window up
map alt+shift+l move_window right
